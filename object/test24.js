// Фильтрация объекта по значению
// Задача:
//     Создайте функцию filterObjectByValue(object, value),
//     которая принимает объект и значение, и возвращает новый объект,
//     содержащий только свойства с этим значением.

// Исходный объект
const data = {
    x: 3,
    y: 1,
    z: 5,
    a: 2,
    b: 5
};

// Значение для фильтрации
const filterValue = 5;

// Ваш код здесь

// Ожидаемый результат: { z: 5, b: 5 }
console.log(filteredObject);
